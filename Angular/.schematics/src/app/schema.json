{
  "title": "app Schematics Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the app?"
    }
  },
  "required": [
    "name"
  ]
}
