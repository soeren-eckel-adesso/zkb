{
  "title": "ui Schematics Schema",
  "type": "object",
  "properties": {
    "customer": {
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the customer?"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the ui?"
    }
  },
  "required": [
    "customer",
    "name"
  ]
}
